<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".dagger2.MyApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SampleFragmentActivity" />
        <activity android:name=".SampleDynamicFragmentActivity" />
        <activity android:name=".SampleWebViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".databinding.DataBindingActivity" />
        <activity android:name=".mvvm.view.UserListActivity" />
        <activity android:name=".dagger2.view.ProductListActivity" />
    </application>

</manifest>